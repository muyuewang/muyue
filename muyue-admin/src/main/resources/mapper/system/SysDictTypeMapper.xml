<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muyue.system.mapper.SysDictTypeMapper">

    <select id="selectDictTypeList" resultType="SysDictType">
        SELECT t.dict_id, t.dict_name, t.dict_type, t.status, t.create_by, t.create_time,
               t.update_by, t.update_time, t.remark
        FROM sys_dict_type t
        <where>
            1 = 1
            <if test="dictType.dictName != null and dictType.dictName != ''">
                AND t.dict_name LIKE '%' || #{dictType.dictName} || '%'
            </if>
            <if test="dictType.dictType != null and dictType.dictType != ''">
                AND t.dict_type LIKE '%' || #{dictType.dictType} || '%'
            </if>
            <if test="dictType.status != null and dictType.status != ''">
                AND t.status = #{dictType.status}
            </if>
        </where>
        ORDER BY t.dict_id
    </select>
</mapper>
