<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muyue.system.mapper.SysMenuMapper">

    <sql id="selectMenuVo">
        SELECT m.menu_id, m.menu_name, m.parent_id, m.order_num, m.path, m.component, m.query,
               m.is_frame, m.is_cache, m.menu_type, m.visible, m.status, m.perms, m.icon,
               m.create_by, m.create_time, m.update_by, m.update_time, m.remark
        FROM sys_menu m
    </sql>

    <select id="selectMenuList" resultType="SysMenu">
        <include refid="selectMenuVo"/>
        <where>
            1 = 1
            <if test="menu.menuName != null and menu.menuName != ''">
                AND m.menu_name LIKE '%' || #{menu.menuName} || '%'
            </if>
            <if test="menu.status != null and menu.status != ''">
                AND m.status = #{menu.status}
            </if>
        </where>
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenuTreeByUserId" resultType="SysMenu">
        SELECT DISTINCT m.menu_id, m.parent_id, m.menu_name, m.order_num, m.path, m.component,
               m.query, m.is_frame, m.is_cache, m.menu_type, m.visible, m.status, m.perms, m.icon,
               m.create_time
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.menu_type IN ('M', 'C')
          AND m.status = '0'
        ORDER BY m.parent_id, m.order_num
    </select>

    <select id="selectMenuPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = '0'
          AND m.perms IS NOT NULL
          AND m.perms &lt;&gt; ''
    </select>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT rm.menu_id
        FROM sys_role_menu rm
        WHERE rm.role_id = #{roleId}
    </select>

    <select id="countRoleMenuByMenuId" resultType="java.lang.Long">
        SELECT COUNT(1) FROM sys_role_menu rm WHERE rm.menu_id = #{menuId}
    </select>

    <delete id="deleteRoleMenuByMenuId">
        DELETE FROM sys_role_menu WHERE menu_id = #{menuId}
    </delete>
</mapper>
